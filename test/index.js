lottie.loadAnimation({
    container: document.getElementById('demo'), // the dom element that will contain the animation: ;
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'test.json' // the path to the animation json
  });